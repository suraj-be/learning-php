<?php 
  /* To create a constant in PHP, you need to define it */

  define('NAME', 'Suraj');
  var_dump(NAME);

  /* The beauty of constants is that they are global variables. That means they can be accessed anywhere within the PHP script, inside functions, inside classes, wherever you want to access this, you can access this, no matter what. */
?>